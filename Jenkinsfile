pipeline {
  agent any
  stages {
    stage('pull images') {
      steps {
        echo 'images have been pulled'
      }
    }

    stage('build images') {
      steps {
        sh 'ls -l'
      }
    }

    stage('deployement') {
      steps {
        echo 'deployement'
      }
    }

  }
}